import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Login and Sign up MobileApp',
  webDir: 'www'
};

export default config;
